<%= form_with(model: message) do |form| %>
  <% if message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(message.errors.count, "error") %> prohibited this message from being saved:</h2>

      <ul>
      <% message.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
  <div class="field">
    <%= form.text_area :message, id:'inputField', placeholder:'Escreva Uma Mensagem...' %>
  </div>

  <div class="actions">
    <%= form.submit class:"btn btn-dark button" %>
  </div>

<% end %>

<%= javascript_include_tag "customText" %>